using System;
using System.Security.Cryptography.X509Certificates;

namespace TechJobsOO
{/*
    public class TestJob
    {
        public int Id { get; }
        private static int nextId = 1;

        public string Name { get; set; }
       /* public Employer EmployerName { get; set; }
        public Location EmployerLocation { get; set; }
        public PositionType JobType { get; set; }
        public CoreCompetency JobCoreCompetency { get; set; }
       */
       /* public TestJob()
        {
            Id = nextId;
            nextId++;
        }

        public TestJob(string name,string empName,string empLoc,string jType,string jobCC) : this()
        {/*
            Name = name;
            EmployerName = new Employer(empName);
            EmployerLocation = new Location(empLoc);
            JobType = new PositionType(jType);
            JobCoreCompetency = new CoreCompetency(jobCC);
            */
        }

        // TODO: Add the two necessary constructors.
        // override object.Equals
        /*public override bool Equals(object obj)
        {
        
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }

            // TODO: write your implementation of Equals() here
            throw new NotImplementedException();
            return base.Equals(obj);
        }

        // override object.GetHashCode
        public override int GetHashCode()
        {
            // TODO: write your implementation of GetHashCode() here
            throw new NotImplementedException();
            return base.GetHashCode();
        }
        // TODO: Generate Equals() and GetHashCode() methods.
    }
*/
